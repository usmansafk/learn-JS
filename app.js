// Numbers -- Additional Operators
// += -= /= *= -- %
// % returns the remainder after integer division

let number = 40;
number += 5;
number -= 10;
number++;
number--;
console.log(number);

const slices = 10;
const children = 4;
// const amount = slices / children;
const amount = slices % children;

console.log(amount);
